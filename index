import React, { useState, useEffect, useRef } from 'react';
import { Github, Linkedin, Mail, Code2, Cpu, Terminal, ArrowRight, ChevronDown } from 'lucide-react';

const Portfolio = () => {
  const [scrollY, setScrollY] = useState(0);
  const [activeSection, setActiveSection] = useState('hero');
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });
  const [isLoaded, setIsLoaded] = useState(false);
  const observerRef = useRef(null);

  // Initialize Intersection Observer for scroll animations
  useEffect(() => {
    setIsLoaded(true);
    
    observerRef.current = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-in');
          }
        });
      },
      { threshold: 0.1, rootMargin: '-50px' }
    );

    // Observe all animatable elements
    document.querySelectorAll('.scroll-animate').forEach(el => {
      observerRef.current?.observe(el);
    });

    return () => observerRef.current?.disconnect();
  }, []);

  useEffect(() => {
    const handleScroll = () => {
      setScrollY(window.scrollY);
      
      const sections = ['hero', 'about', 'skills', 'projects', 'education', 'contact'];
      const current = sections.find(section => {
        const element = document.getElementById(section);
        if (element) {
          const rect = element.getBoundingClientRect();
          return rect.top <= 150 && rect.bottom >= 150;
        }
        return false;
      });
      if (current) setActiveSection(current);
    };

    const handleMouseMove = (e) => {
      setMousePosition({ x: e.clientX, y: e.clientY });
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    window.addEventListener('mousemove', handleMouseMove, { passive: true });
    
    return () => {
      window.removeEventListener('scroll', handleScroll);
      window.removeEventListener('mousemove', handleMouseMove);
    };
  }, []);

  const scrollToSection = (id) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
  };

  const projects = [
    {
      title: "Intelligent Task Scheduler",
      description: "AI-powered scheduling system optimizing task allocation using custom algorithms and data structures.",
      tech: ["C++", "Python", "Algorithms"],
      gradient: "from-blue-600 via-blue-500 to-cyan-500"
    },
    {
      title: "Real-Time Systems Monitor",
      description: "Low-level system monitoring tool tracking CPU, memory, and process metrics with visual analytics.",
      tech: ["C++", "Systems Programming", "Linux"],
      gradient: "from-purple-600 via-purple-500 to-pink-500"
    },
    {
      title: "Academic Performance Analyzer",
      description: "Data-driven platform analyzing student performance patterns and generating insights.",
      tech: ["Python", "Data Analysis", "Web"],
      gradient: "from-emerald-600 via-emerald-500 to-teal-500"
    }
  ];

  const skills = {
    languages: ["C++", "Python", "JavaScript", "HTML/CSS", "SQL"],
    tools: ["Git", "Linux", "VS Code", "Docker", "Jupyter"],
    engineering: ["Data Structures", "Algorithms", "System Design", "Problem Solving", "Debugging"]
  };

  const parallaxY = scrollY * 0.5;
  const heroOpacity = Math.max(0, 1 - scrollY / 500);

  return (
    <div className="bg-black text-white min-h-screen overflow-x-hidden">
      {/* Magnetic Cursor Glow - Only visible in hero */}
      <div 
        className="cursor-glow pointer-events-none fixed z-50 transition-opacity duration-700"
        style={{
          left: mousePosition.x - 192,
          top: mousePosition.y - 192,
          opacity: scrollY < 100 ? heroOpacity * 0.6 : 0
        }}
      />

      {/* Premium Navigation Bar */}
      <nav 
        className="nav-bar fixed top-0 w-full z-40"
        style={{
          background: scrollY > 50 ? 'rgba(0, 0, 0, 0.85)' : 'transparent',
          backdropFilter: scrollY > 50 ? 'blur(20px)' : 'none',
          borderBottom: scrollY > 50 ? '1px solid rgba(34, 211, 238, 0.15)' : 'none'
        }}
      >
        <div className="max-w-7xl mx-auto px-6 lg:px-12 py-6 flex justify-between items-center">
          <div className="logo text-2xl font-black tracking-tighter">
            <span className="bg-gradient-to-r from-white to-cyan-400 bg-clip-text text-transparent">YS</span>
          </div>
          <div className="hidden md:flex gap-12 text-sm font-bold tracking-wider uppercase">
            {['About', 'Skills', 'Projects', 'Education', 'Contact'].map((item, i) => (
              <button
                key={item}
                onClick={() => scrollToSection(item.toLowerCase())}
                className="nav-link relative hover:text-cyan-400 transition-colors duration-300"
                style={{
                  color: activeSection === item.toLowerCase() ? '#22d3ee' : 'inherit',
                  animationDelay: `${i * 0.1}s`
                }}
              >
                {item}
                {activeSection === item.toLowerCase() && (
                  <span className="nav-underline absolute -bottom-2 left-0 w-full h-0.5 bg-gradient-to-r from-transparent via-cyan-400 to-transparent" />
                )}
              </button>
            ))}
          </div>
        </div>
      </nav>

      {/* Hero Section - Cinematic Reveal */}
      <section id="hero" className="hero-section relative min-h-screen flex items-center justify-center overflow-hidden">
        {/* Animated Background Layers */}
        <div className="absolute inset-0">
          <div className="absolute inset-0 bg-gradient-to-br from-cyan-500/10 via-transparent to-purple-500/10" />
          <div 
            className="grid-overlay absolute inset-0 opacity-20"
            style={{ transform: `translateY(${parallaxY}px)` }}
          />
        </div>

        {/* Floating Ambient Orbs */}
        <div className="orb orb-1 absolute top-1/4 left-1/4" />
        <div className="orb orb-2 absolute bottom-1/4 right-1/4" />

        <div className="relative z-10 text-center px-6 max-w-7xl mx-auto" style={{ opacity: heroOpacity }}>
          {/* Top Label - Slide Down */}
          <div className="hero-label overflow-hidden mb-8">
            <span className="inline-block px-6 py-2 border border-cyan-400/30 rounded-full text-cyan-400 text-sm font-bold tracking-widest uppercase backdrop-blur-sm">
              Computer Engineering Student
            </span>
          </div>

          {/* Name Animation - Letter by Letter Reveal */}
          <div className="overflow-hidden mb-6">
            <h1 className="hero-title font-black tracking-tighter leading-none"
                style={{
                  fontSize: 'clamp(3.5rem, 15vw, 12rem)',
                  fontWeight: 900,
                  letterSpacing: '-0.05em'
                }}>
              <span className="hero-name-line block">
                {'YAZAN'.split('').map((letter, i) => (
                  <span key={i} className="hero-letter inline-block" style={{ animationDelay: `${i * 0.05}s` }}>
                    {letter}
                  </span>
                ))}
              </span>
              <span className="hero-name-line block">
                {'SALAMEH'.split('').map((letter, i) => (
                  <span key={i} className="hero-letter inline-block" style={{ animationDelay: `${(i + 5) * 0.05}s` }}>
                    {letter}
                  </span>
                ))}
              </span>
            </h1>
          </div>

          {/* Subtitle - Fade + Slide */}
          <p className="hero-subtitle text-xl md:text-3xl font-bold text-gray-400 mb-6 tracking-tight max-w-3xl mx-auto leading-relaxed">
            Building the future through{' '}
            <span className="text-cyan-400">code</span>,{' '}
            <span className="text-purple-400">systems</span>, and{' '}
            <span className="text-emerald-400">innovation</span>
          </p>

          {/* CTA Buttons - Staggered Fade */}
          <div className="hero-cta flex flex-col sm:flex-row gap-6 justify-center items-center mt-12">
            <a 
              href="#contact" 
              className="magnetic-button group relative px-10 py-4 bg-gradient-to-r from-cyan-500 to-cyan-400 hover:from-cyan-400 hover:to-cyan-300 text-black font-black text-lg tracking-wide uppercase overflow-hidden"
            >
              <span className="relative z-10 flex items-center gap-2">
                Get in Touch
                <ArrowRight className="w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" />
              </span>
              <div className="button-shine absolute inset-0 bg-white/20 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-500" />
            </a>
            <button 
              onClick={() => scrollToSection('projects')} 
              className="magnetic-button group px-10 py-4 border-2 border-white/30 hover:border-cyan-400 font-black text-lg tracking-wide uppercase backdrop-blur-sm"
            >
              <span className="flex items-center gap-2">
                View Projects
                <ArrowRight className="w-5 h-5 transition-transform duration-300 group-hover:translate-x-1" />
              </span>
            </button>
          </div>
        </div>

        {/* Scroll Indicator - Breathing Animation */}
        <div className="scroll-indicator absolute bottom-12 left-1/2 transform -translate-x-1/2">
          <div className="flex flex-col items-center gap-2 text-gray-500">
            <span className="text-xs font-bold tracking-widest uppercase">Scroll</span>
            <ChevronDown className="w-6 h-6" />
          </div>
        </div>
      </section>

      {/* About Section - Staggered Paragraphs */}
      <section id="about" className="py-40 px-6 relative">
        <div className="absolute inset-0 bg-gradient-to-b from-black via-cyan-950/10 to-black" />
        <div className="max-w-5xl mx-auto relative z-10">
          <h2 className="scroll-animate section-title text-6xl md:text-8xl font-black mb-16 tracking-tighter leading-none">
            <span className="bg-gradient-to-r from-white to-cyan-400 bg-clip-text text-transparent">
              About Me
            </span>
          </h2>
          
          <div className="space-y-8 text-xl md:text-2xl text-gray-300 leading-relaxed font-medium">
            <p className="scroll-animate paragraph">
              I'm a Computer Engineering student at <span className="text-white font-bold">Palestine Technical University – Kadoorie (PTUK)</span>, 
              driven by a passion for understanding how technology shapes our world.
            </p>
            <p className="scroll-animate paragraph" style={{ animationDelay: '0.1s' }}>
              From <span className="text-cyan-400 font-bold">low-level system architecture</span> to <span className="text-purple-400 font-bold">high-level software solutions</span>, 
              I'm fascinated by the entire spectrum of computing.
            </p>
            <p className="scroll-animate paragraph" style={{ animationDelay: '0.2s' }}>
              My journey in engineering is fueled by <span className="text-emerald-400 font-bold">curiosity</span> and a 
              relentless drive to build meaningful solutions that push the boundaries of what's possible.
            </p>
          </div>
        </div>
      </section>

      {/* Skills Section - Card Reveal */}
      <section id="skills" className="py-40 px-6 relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-b from-black via-purple-950/10 to-black" />
        
        <div className="max-w-6xl mx-auto relative z-10">
          <h2 className="scroll-animate section-title text-6xl md:text-8xl font-black mb-20 tracking-tighter leading-none">
            <span className="bg-gradient-to-r from-white to-purple-400 bg-clip-text text-transparent">
              Skills & Expertise
            </span>
          </h2>
          
          <div className="grid md:grid-cols-3 gap-12">
            {[
              { title: "Languages", icon: Code2, items: skills.languages, color: "cyan" },
              { title: "Tools & Tech", icon: Terminal, items: skills.tools, color: "purple" },
              { title: "Engineering", icon: Cpu, items: skills.engineering, color: "emerald" }
            ].map((category, idx) => (
              <div key={idx} className="scroll-animate skill-card">
                <div className="flex items-center gap-4 mb-8">
                  <div className={`icon-box p-3 bg-gradient-to-br from-${category.color}-500/20 to-${category.color}-600/20 border border-${category.color}-500/30`}>
                    <category.icon className={`w-8 h-8 text-${category.color}-400`} />
                  </div>
                  <h3 className="text-3xl font-black tracking-tight">{category.title}</h3>
                </div>
                <div className="space-y-4">
                  {category.items.map((skill, i) => (
                    <div 
                      key={i} 
                      className="skill-item group/item flex items-center gap-4 p-4 border border-white/5 hover:border-white/20 backdrop-blur-sm"
                      style={{ animationDelay: `${i * 0.05}s` }}
                    >
                      <div className={`skill-dot w-2 h-2 bg-${category.color}-400 rounded-full`} />
                      <span className="text-lg font-bold text-gray-400 group-hover/item:text-white transition-colors duration-300">
                        {skill}
                      </span>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Projects Section - Card Cascade */}
      <section id="projects" className="py-40 px-6 relative">
        <div className="absolute inset-0 bg-gradient-to-b from-black via-emerald-950/10 to-black" />
        
        <div className="max-w-7xl mx-auto relative z-10">
          <h2 className="scroll-animate section-title text-6xl md:text-8xl font-black mb-20 tracking-tighter leading-none">
            <span className="bg-gradient-to-r from-white to-emerald-400 bg-clip-text text-transparent">
              Selected Projects
            </span>
          </h2>
          
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {projects.map((project, i) => (
              <div
                key={i}
                className="scroll-animate project-card group relative overflow-hidden border border-white/10 backdrop-blur-sm"
                style={{ 
                  animationDelay: `${i * 0.15}s`,
                  background: 'rgba(0, 0, 0, 0.5)'
                }}
              >
                {/* Gradient Overlay */}
                <div className={`card-gradient absolute inset-0 bg-gradient-to-br ${project.gradient} opacity-0`} />
                
                {/* Glow Effect */}
                <div className={`card-glow absolute inset-0 bg-gradient-to-br ${project.gradient} opacity-0 blur-xl -z-10`} />
                
                <div className="relative z-10 p-8">
                  <h3 className="card-title text-2xl md:text-3xl font-black mb-4 tracking-tight">
                    {project.title}
                  </h3>
                  <p className="text-gray-400 mb-6 leading-relaxed font-medium">
                    {project.description}
                  </p>
                  <div className="flex flex-wrap gap-2">
                    {project.tech.map((t, j) => (
                      <span 
                        key={j} 
                        className="tech-badge px-4 py-1.5 text-xs font-bold border border-white/20 rounded-full text-gray-300 uppercase tracking-wider"
                      >
                        {t}
                      </span>
                    ))}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Education Section - Timeline Draw */}
      <section id="education" className="py-40 px-6 relative">
        <div className="absolute inset-0 bg-gradient-to-b from-black via-blue-950/10 to-black" />
        
        <div className="max-w-5xl mx-auto relative z-10">
          <h2 className="scroll-animate section-title text-6xl md:text-8xl font-black mb-20 tracking-tighter leading-none">
            <span className="bg-gradient-to-r from-white to-blue-400 bg-clip-text text-transparent">
              Education
            </span>
          </h2>
          
          <div className="scroll-animate education-timeline relative pl-12 border-l-4 border-cyan-400/30">
            <div className="timeline-dot absolute -left-3 top-0 w-6 h-6 bg-gradient-to-br from-cyan-400 to-cyan-600 rounded-full shadow-lg shadow-cyan-400/50" />
            
            <div className="space-y-2">
              <h3 className="text-4xl md:text-5xl font-black tracking-tight">Computer Engineering</h3>
              <p className="text-2xl md:text-3xl font-bold text-cyan-400 mb-6">
                Palestine Technical University – Kadoorie (PTUK)
              </p>
              <p className="text-xl text-gray-300 leading-relaxed font-medium">
                Building a strong foundation in <span className="text-white font-bold">software development</span>, 
                <span className="text-white font-bold"> computer systems architecture</span>, and 
                <span className="text-white font-bold"> engineering problem-solving</span>. 
                Mastering both theoretical concepts and practical applications to drive innovation in technology.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Contact Section - Radial Reveal */}
      <section id="contact" className="py-40 px-6 relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-b from-black via-cyan-950/20 to-black" />
        
        {/* Glow Orb */}
        <div className="contact-glow absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2" />
        
        <div className="max-w-4xl mx-auto text-center relative z-10">
          <h2 className="scroll-animate section-title text-6xl md:text-8xl font-black mb-8 tracking-tighter leading-none">
            <span className="bg-gradient-to-r from-white via-cyan-400 to-purple-400 bg-clip-text text-transparent">
              Let's Connect
            </span>
          </h2>
          
          <p className="scroll-animate text-xl md:text-2xl font-bold text-gray-400 mb-16 max-w-2xl mx-auto leading-relaxed">
            Open to discussing opportunities, collaborations, or conversations about 
            <span className="text-cyan-400"> technology</span> and 
            <span className="text-purple-400"> innovation</span>
          </p>
          
          <div className="scroll-animate flex justify-center gap-6 mb-16">
            {[
              { icon: Mail, href: "mailto:yazan@example.com", label: "Email" },
              { icon: Github, href: "https://github.com", label: "GitHub" },
              { icon: Linkedin, href: "https://linkedin.com", label: "LinkedIn" }
            ].map((social, i) => (
              <a 
                key={i}
                href={social.href} 
                target={social.href.startsWith('http') ? "_blank" : undefined}
                rel={social.href.startsWith('http') ? "noopener noreferrer" : undefined}
                className="social-icon group relative p-6 border-2 border-white/20 overflow-hidden"
                aria-label={social.label}
                style={{ animationDelay: `${i * 0.1}s` }}
              >
                <div className="social-glow absolute inset-0 bg-gradient-to-br from-cyan-400/0 to-cyan-400/0" />
                <social.icon className="w-7 h-7 relative z-10 transition-transform duration-500 group-hover:scale-125" />
              </a>
            ))}
          </div>
          
          <a 
            href="mailto:yazan@example.com" 
            className="scroll-animate magnetic-button group inline-flex items-center gap-3 px-12 py-5 bg-gradient-to-r from-cyan-500 to-purple-500 hover:from-cyan-400 hover:to-purple-400 text-black font-black text-xl tracking-wide uppercase overflow-hidden"
          >
            <span className="relative z-10">Send Message</span>
            <ArrowRight className="w-6 h-6 relative z-10 transition-transform duration-300 group-hover:translate-x-2" />
            <div className="button-shine absolute inset-0 bg-white/20 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-500" />
          </a>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-12 px-6 border-t border-white/10 relative">
        <div className="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-center gap-4">
          <p className="text-gray-500 font-bold tracking-wide">© 2026 Yazan Salameh</p>
          <p className="text-gray-500 font-bold tracking-wide">Built with Passion & Precision</p>
        </div>
      </footer>

      {/* CSS Animations - GPU Optimized */}
      <style jsx>{`
        /* ========================================
           PERFORMANCE OPTIMIZATION
           - Using transform & opacity only (GPU accelerated)
           - will-change for smoother animations
           - Respect prefers-reduced-motion
        ======================================== */

        @media (prefers-reduced-motion: reduce) {
          *, *::before, *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
          }
        }

        * {
          scroll-behavior: smooth;
        }

        /* ========================================
           CURSOR GLOW - Magnetic Effect
        ======================================== */
        .cursor-glow {
          width: 384px;
          height: 384px;
          background: radial-gradient(circle, rgba(34, 211, 238, 0.15) 0%, transparent 70%);
          transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* ========================================
           NAVIGATION - Smooth Reveal
        ======================================== */
        .nav-bar {
          transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .logo {
          animation: fadeSlideDown 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .nav-link {
          animation: fadeSlideDown 0.8s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .nav-underline {
          animation: expandWidth 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes expandWidth {
          from { transform: scaleX(0); }
          to { transform: scaleX(1); }
        }

        /* ========================================
           HERO SECTION - Cinematic Entrance
        ======================================== */
        .hero-section {
          will-change: transform;
        }

        .grid-overlay {
          background-image: 
            linear-gradient(rgba(34, 211, 238, 0.1) 1px, transparent 1px),
            linear-gradient(90deg, rgba(34, 211, 238, 0.1) 1px, transparent 1px);
          background-size: 100px 100px;
          will-change: transform;
        }

        .orb {
          width: 384px;
          height: 384px;
          border-radius: 50%;
          filter: blur(64px);
          will-change: transform;
        }

        .orb-1 {
          background: rgba(34, 211, 238, 0.2);
          animation: float 20s ease-in-out infinite;
        }

        .orb-2 {
          background: rgba(168, 85, 247, 0.2);
          animation: floatReverse 25s ease-in-out infinite;
        }

        @keyframes float {
          0%, 100% { transform: translate(0, 0) scale(1); }
          50% { transform: translate(30px, -30px) scale(1.1); }
        }

        @keyframes floatReverse {
          0%, 100% { transform: translate(0, 0) scale(1); }
          50% { transform: translate(-30px, 30px) scale(1.1); }
        }

        /* Hero Label - Slide Down */
        .hero-label {
          animation: fadeSlideDown 1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s both;
        }

        @keyframes fadeSlideDown {
          from {
            opacity: 0;
            transform: translateY(-30px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        /* Hero Title - Letter Reveal */
        .hero-letter {
          animation: letterReveal 0.8s cubic-bezier(0.16, 1, 0.3, 1) both;
          will-change: transform, opacity;
        }

        .hero-name-line .hero-letter {
          color: #fff;
        }

        @keyframes letterReveal {
          from {
            opacity: 0;
            transform: translateY(50px) rotateX(-90deg);
          }
          to {
            opacity: 1;
            transform: translateY(0) rotateX(0);
          }
        }

        /* Hero Subtitle */
        .hero-subtitle {
          animation: fadeSlideUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.8s both;
        }

        /* Hero CTA */
        .hero-cta {
          animation: fadeSlideUp 1s cubic-bezier(0.16, 1, 0.3, 1) 1s both;
        }

        @keyframes fadeSlideUp {
          from {
            opacity: 0;
            transform: translateY(30px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        /* Scroll Indicator */
        .scroll-indicator {
          animation: breathe 3s ease-in-out infinite;
        }

        @keyframes breathe {
          0%, 100% { transform: translate(-50%, 0); opacity: 1; }
          50% { transform: translate(-50%, -10px); opacity: 0.7; }
        }

        /* ========================================
           BUTTONS - Magnetic + Shine
        ======================================== */
        .magnetic-button {
          transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .magnetic-button:hover {
          transform: scale(1.05) translateY(-2px);
          box-shadow: 0 20px 40px rgba(34, 211, 238, 0.3);
        }

        .magnetic-button:active {
          transform: scale(0.98);
        }

        .button-shine {
          will-change: transform;
        }

        /* ========================================
           SCROLL ANIMATIONS - IntersectionObserver
        ======================================== */
        .scroll-animate {
          opacity: 0;
          transform: translateY(50px);
          transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                      transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform, opacity;
        }

        .scroll-animate.animate-in {
          opacity: 1;
          transform: translateY(0);
        }

        /* Section Titles - Extra Drama */
        .section-title {
          transform: translateY(80px);
        }

        .section-title.animate-in {
          transform: translateY(0);
        }

        /* Paragraphs - Staggered */
        .paragraph:nth-child(1) { transition-delay: 0.1s; }
        .paragraph:nth-child(2) { transition-delay: 0.2s; }
        .paragraph:nth-child(3) { transition-delay: 0.3s; }

        /* ========================================
           SKILLS SECTION - Card Cascade
        ======================================== */
        .skill-card {
          transform: translateY(60px) scale(0.95);
        }

        .skill-card.animate-in {
          transform: translateY(0) scale(1);
        }

        .skill-card:nth-child(1) { transition-delay: 0.1s; }
        .skill-card:nth-child(2) { transition-delay: 0.2s; }
        .skill-card:nth-child(3) { transition-delay: 0.3s; }

        .icon-box {
          transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .skill-card:hover .icon-box {
          transform: scale(1.1) rotate(5deg);
        }

        .skill-item {
          transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .skill-item:hover {
          transform: translateX(8px);
          background: rgba(255, 255, 255, 0.05);
        }

        .skill-dot {
          transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .skill-item:hover .skill-dot {
          transform: scale(1.5);
          box-shadow: 0 0 12px currentColor;
        }

        /* ========================================
           PROJECTS SECTION - Card Lift
        ======================================== */
        .project-card {
          transform: translateY(60px) scale(0.9);
          transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform, opacity;
        }

        .project-card.animate-in {
          transform: translateY(0) scale(1);
        }

        .project-card:hover {
          transform: translateY(-12px) scale(1.02);
          border-color: rgba(34, 211, 238, 0.5);
        }

        .card-gradient {
          transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .project-card:hover .card-gradient {
          opacity: 0.2;
        }

        .card-glow {
          transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .project-card:hover .card-glow {
          opacity: 1;
        }

        .card-title {
          transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .project-card:hover .card-title {
          background: linear-gradient(to right, #fff, #22d3ee);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
        }

        .tech-badge {
          transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .project-card:hover .tech-badge {
          border-color: rgba(255, 255, 255, 0.4);
          color: #fff;
          transform: translateY(-2px);
        }

        /* ========================================
           EDUCATION - Timeline Draw
        ======================================== */
        .education-timeline {
          opacity: 0;
          transform: translateX(-50px);
        }

        .education-timeline.animate-in {
          opacity: 1;
          transform: translateX(0);
          transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .education-timeline.animate-in::before {
          content: '';
          position: absolute;
          left: -4px;
          top: 0;
          width: 4px;
          height: 100%;
          background: linear-gradient(to bottom, #22d3ee, transparent);
          animation: drawLine 1.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes drawLine {
          from { height: 0; }
          to { height: 100%; }
        }

        .timeline-dot {
          animation: pulseDot 3s ease-in-out infinite;
        }

        @keyframes pulseDot {
          0%, 100% { transform: scale(1); opacity: 1; }
          50% { transform: scale(1.2); opacity: 0.8; }
        }

        /* ========================================
           CONTACT - Social Icons
        ======================================== */
        .contact-glow {
          width: 800px;
          height: 800px;
          background: radial-gradient(circle, rgba(34, 211, 238, 0.1) 0%, transparent 70%);
          filter: blur(64px);
        }

        .social-icon {
          transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
          will-change: transform;
        }

        .social-icon:hover {
          transform: scale(1.15) translateY(-4px);
          border-color: #22d3ee;
          box-shadow: 0 10px 30px rgba(34, 211, 238, 0.3);
        }

        .social-glow {
          transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .social-icon:hover .social-glow {
          background: linear-gradient(135deg, rgba(34, 211, 238, 0.2), rgba(168, 85, 247, 0.2));
        }

        /* ========================================
           SCROLLBAR - Branded
        ======================================== */
        ::-webkit-scrollbar {
          width: 12px;
        }
        
        ::-webkit-scrollbar-track {
          background: #000;
        }
        
        ::-webkit-scrollbar-thumb {
          background: linear-gradient(to bottom, #22d3ee, #a855f7);
          border-radius: 6px;
          transition: background 0.3s ease;
        }
        
        ::-webkit-scrollbar-thumb:hover {
          background: linear-gradient(to bottom, #06b6d4, #9333ea);
        }

        /* ========================================
           MICRO-INTERACTIONS
        ======================================== */
        
        /* Link Hover */
        a:not(.magnetic-button):not(.social-icon) {
          transition: color 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* Focus States for Accessibility */
        button:focus-visible,
        a:focus-visible {
          outline: 2px solid #22d3ee;
          outline-offset: 4px;
        }
      `}</style>
    </div>
  );
};

export default Portfolio;
